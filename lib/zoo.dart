import 'package:flutter/material.dart';

class MyZoo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: "My Zoos",
      theme: ThemeData(brightness: Brightness.dark),
      home: Scaffold(),
    );
  }
}
